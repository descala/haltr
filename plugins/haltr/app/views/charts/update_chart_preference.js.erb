$('div.<%=@chart_name%>').replaceWith("<%= escape_javascript(render(partial: "my/blocks/#{@chart_name}")).html_safe %>")

//TODO duplicated code in charts.js
$("select.chart_prefs").change(function() {
  var id = $(this).children(":selected").val();
  var params = 'name='+$(this)[0].id+'&value='+id;
  $.ajax({
    url: $(this).data('url'),
    data: params
  })
})

