<%=
  simple_format(@body.gsub(/@invoice_url/) {|s|
    link = invoice_public_view_url(:invoice_id=>@invoice.id,
                                   :client_hashid=>@invoice.client.hashid)
    link_to link, link
  })
%>

<%= call_hook(:invoice_mail_footer) %>
