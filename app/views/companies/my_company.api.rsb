# vi: ft=ruby

api.my_company do
  %w(taxcode name email phone address city postalcode province country website
  invoice_format currency company_identifier).each do |attr|
    api.send(attr, @company.send(attr))
  end
  api.logo url_for(
    {controller: 'companies', action: 'logo', project_id: @company.project}
  )
end
