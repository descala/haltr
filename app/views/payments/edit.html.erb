<h2><%= l(:label_payment) %></h2>

<% labelled_tabular_form_for :payment, @payment, :url => { :action => 'update', :id => @payment } do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<%= submit_tag l(:button_save) %>
<% end %>
