<!--formulari-->
<div class="seccio grup">
  <div class="col span-2-de-2">
    <%= labelled_form_for :client, @client,
      :url => {:action => 'create', :id => @project},
      :html => {:class => 'formee'} do |f| %>
      <%= render :partial => 'form', :locals => { :f => f } %>
      <%= submit_tag l(:button_create) %>
    <% end %>
  </div>
</div>
<!--final formulari-->
