<%= render :partial => 'common/chart_headers' %>

<% haltr_projects.each do |project| %>
  <h3><%=l('chart_top_clients')%> (<%= project.company.name %>)</h4>
<%= column_chart top_clients_chart_path(project), library: {isStacked: true, vAxis: {title: "#{l(:invoice_total)} (€)" }} %>
<% end %>
